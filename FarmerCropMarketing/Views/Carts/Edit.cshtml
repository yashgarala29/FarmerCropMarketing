@model FarmerCropMarketing.Models.Cart

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Cart</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Cart_id" />
            <div class="form-group">
                <label asp-for="Farmers_id" class="control-label"></label>
                <select asp-for="Farmers_id" class="form-control" asp-items="ViewBag.Farmers_id"></select>
                <span asp-validation-for="Farmers_id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Crops_id" class="control-label"></label>
                <select asp-for="Crops_id" class="form-control" asp-items="ViewBag.Crops_id"></select>
                <span asp-validation-for="Crops_id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
